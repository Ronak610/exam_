import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class Database_Screen extends StatefulWidget {
  const Database_Screen({Key? key}) : super(key: key);

  @override
  State<Database_Screen> createState() => _Database_ScreenState();
}

class _Database_ScreenState extends State<Database_Screen> {
  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        appBar: AppBar(
          title: Text("------------"),
        ),
        body:,
      ),
    );
  }
}
